import { GenerateHTMLDirective } from './generate-html.directive';

describe('GenerateHTMLDirective', () => {
  it('should create an instance', () => {
    const directive = new GenerateHTMLDirective();
    expect(directive).toBeTruthy();
  });
});
