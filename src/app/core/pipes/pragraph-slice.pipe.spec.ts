import { PragraphSlicePipe } from './pragraph-slice.pipe';

describe('PragraphSlicePipe', () => {
  it('create an instance', () => {
    const pipe = new PragraphSlicePipe();
    expect(pipe).toBeTruthy();
  });
});
