import { TranslateToArabicPipe } from './translate-to-arabic.pipe';

describe('TranslateToArabicPipe', () => {
  it('create an instance', () => {
    const pipe = new TranslateToArabicPipe();
    expect(pipe).toBeTruthy();
  });
});
