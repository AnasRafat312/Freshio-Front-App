import { WebDataEnumsToStringPipe } from './web-data-enums-to-string.pipe';

describe('WebDataEnumsToStringPipe', () => {
  it('create an instance', () => {
    const pipe = new WebDataEnumsToStringPipe();
    expect(pipe).toBeTruthy();
  });
});
